# raws

Только объявление источников (sources). Файлы вида `_raws_<домен>.yml` или `_raws_<домен>__<сабдомен>.yml`.

Моделей в этом слое нет — сырые таблицы создаются через `dbt_external_tables` (external tables) или объявляются как source на существующие таблицы.
